# Application
server:
    port: ${SERVER_PORT}

# OpenAPI
springdoc:
    api-docs:
        enabled: true
        path: /v3/api-docs
    swagger-ui:
        enabled: true
        path: /swagger-ui.html

# Logging
logging:
    level:
        org.springframework: ERROR
        io.swagger.v3.oas: DEBUG